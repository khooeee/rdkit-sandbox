# RDKit Sandbox

This project uses [`uv`](https://docs.astral.sh/uv/) and Python 3.13.

## aspirin

A small RDKit sandbox that builds an aspirin molecule from SMILES

```sh
uv sync
uv run python aspirin.py
```

Running the script updates `aspirin.png` in the project root.
